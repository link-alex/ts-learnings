# React redux with TS app

Simple react/redux app with TS which loads list of Todos, displays them and deletes by click.

### Installation

```
npm i
npm start
```

Covered some basics of:

- Usage of react/redux together with TS
